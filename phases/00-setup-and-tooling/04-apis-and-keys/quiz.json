{
  "questions": [
    {
      "stage": "pre",
      "question": "What is an API key used for when calling an LLM service?",
      "options": ["Encrypting the data sent to the server", "Authenticating your identity and authorizing requests", "Compressing requests to reduce bandwidth", "Selecting which programming language the server uses"],
      "correct": 1,
      "explanation": "An API key is a unique string that identifies your account, authorizes your requests, and allows the provider to track usage and billing."
    },
    {
      "stage": "pre",
      "question": "Why should API keys never be hardcoded directly in source code?",
      "options": ["Python cannot read string literals longer than 50 characters", "API providers block keys that appear in source files", "Hardcoded keys make the code run slower", "They can be accidentally committed to git and exposed publicly"],
      "correct": 3,
      "explanation": "Hardcoded keys in source code risk being committed to version control and pushed to public repositories, where they can be scraped and abused by others."
    },
    {
      "stage": "post",
      "question": "What is the recommended way to store API keys for local development?",
      "options": ["In a .env file that is listed in .gitignore", "In the README.md for easy access", "In a Python variable at the top of your script", "In the requirements.txt alongside package versions"],
      "correct": 0,
      "explanation": "A .env file stores keys as environment variables and should be added to .gitignore so it is never committed to version control. SDKs read keys from environment variables automatically."
    },
    {
      "stage": "post",
      "question": "In the Gemini generateContent request used in this lesson, where is the API key sent?",
      "options": ["In the prompt text", "In the x-goog-api-key header", "In the model name", "In the response body"],
      "correct": 1,
      "explanation": "This lesson sends GEMINI_API_KEY as the x-goog-api-key header. Never put the key in the prompt, source code, or a copied URL."
    },
    {
      "stage": "post",
      "question": "What happens when you exceed an API's rate limit?",
      "options": ["Your request is queued and processed later automatically", "The server returns an error (typically HTTP 429) and you must wait before retrying", "Your API key is permanently revoked", "The API silently drops your request with no response"],
      "correct": 1,
      "explanation": "Rate limiting returns HTTP 429 (Too Many Requests). You need to wait and retry, typically with exponential backoff. Rate limits prevent abuse and ensure fair usage."
    },
    {
      "stage": "check",
      "question": "Where does Gemini put generated text in a successful response?",
      "options": ["content[0].text", "message.text", "candidates[0].content.parts[0].text", "output.text"],
      "correct": 2,
      "explanation": "Gemini returns one or more candidates. The text for the first candidate is nested at candidates[0].content.parts[0].text."
    }
  ]
}
